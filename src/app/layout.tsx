import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "NaturalSoap Co. - Premium Handmade Natural Soaps",
  description: "Discover our collection of premium handmade natural soaps. Made with organic ingredients for healthy, beautiful skin. Now expanding to more natural products.",
  keywords: "natural soap, handmade soap, organic soap, skincare, natural products",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen flex flex-col">
          <Header />
          <main className="flex-grow">
            {children}
          </main>
          <Footer />
        </div>
      </body>
    </html>
  );
}